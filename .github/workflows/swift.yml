name: Swift

on:
  push:
    branches: [ master]
  pull_request:
    branches: [ master]

jobs:    
  test:
    name: Test
    runs-on: macos-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Bundle Install
      run: bundle install
    - name: tests
      run: bundle exec fastlane tests
